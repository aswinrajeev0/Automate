import { Route, Routes } from "react-router-dom";

export const WorkshopRoutes = () => {
    return (
        <Routes>
            <Route path="/" />
            <Route path="/login" />
            <Route path="/signup" />
        </Routes>
    )
}